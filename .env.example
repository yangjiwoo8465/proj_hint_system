# ========================================
# 프로젝트 환경 설정 예시 파일
# ========================================
# 이 파일을 복사해서 ".env" 파일로 만들고
# 본인 환경에 맞게 경로를 수정하세요.
#
# 사용법:
# 1. 이 파일을 복사: cp .env.example .env
# 2. .env 파일을 열어서 경로 수정
# 3. Python에서 자동으로 불러옴
# ========================================

# ========================================
# 1. 프로젝트 루트 경로
# ========================================
# 이 프로젝트 폴더의 절대 경로를 입력하세요.
# 예시 (Windows): C:\Users\username\Desktop\5th-project_mvp
# 예시 (Mac/Linux): /Users/username/Desktop/5th-project_mvp
PROJECT_ROOT=C:\Users\playdata2\Desktop\playdata\Workspace\팀프로젝트5\5th-project_mvp

# ========================================
# 2. 데이터 경로
# ========================================
# 힌트 시스템에서 사용할 문제 데이터 파일 경로
# 기본값: {PROJECT_ROOT}/hint-system/data/problems_multi_solution.json
DATA_FILE_PATH=${PROJECT_ROOT}/hint-system/data/problems_multi_solution.json

# 크롤러 출력 디렉토리
# 기본값: {PROJECT_ROOT}/crawler/data/raw
CRAWLER_OUTPUT_DIR=${PROJECT_ROOT}/crawler/data/raw

# ========================================
# 3. 평가 결과 저장 경로
# ========================================
# 힌트 평가 결과를 저장할 디렉토리
# 기본값: {PROJECT_ROOT}/hint-system/evaluation/results
EVALUATION_RESULTS_DIR=${PROJECT_ROOT}/hint-system/evaluation/results

# ========================================
# 4. 모델 설정 (선택사항)
# ========================================
# vLLM 서버 주소 (로컬인 경우 기본값 사용)
VLLM_SERVER_URL=http://localhost:8000

# 기본 사용할 모델
DEFAULT_MODEL=Qwen/Qwen2.5-Coder-1.5B-Instruct

# 기본 Temperature (0.1 ~ 1.0)
DEFAULT_TEMPERATURE=0.7

# ========================================
# 5. Runpod 원격 모델 설정 (선택사항)
# ========================================
# Runpod을 사용해서 무거운 모델을 원격으로 실행할 수 있습니다.
# USE_RUNPOD=true로 설정하면 로컬 모델 대신 Runpod API를 사용합니다.

# Runpod 사용 여부 (true/false)
USE_RUNPOD=false

# Runpod API 엔드포인트 (Runpod에서 발급받은 URL)
# 예시: https://api.runpod.ai/v2/YOUR_ENDPOINT_ID/runsync
RUNPOD_API_ENDPOINT=

# Runpod API 키 (Runpod 대시보드에서 발급)
RUNPOD_API_KEY=

# ========================================
# 6. API 키 (선택사항)
# ========================================
# solved.ac API 키 (크롤링 속도 향상용, 선택사항)
# https://solved.ac/api 에서 발급
SOLVED_AC_API_KEY=

# ========================================
# 6. 로그 설정 (선택사항)
# ========================================
# 로그 레벨: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# 로그 파일 경로
LOG_FILE_PATH=${PROJECT_ROOT}/logs/app.log
